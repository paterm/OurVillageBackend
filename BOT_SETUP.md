# Настройка и запуск Telegram бота

## Проблема: Бот не обрабатывает команду /start

Если Telegram открывается, но авторизация не происходит, значит бот не запущен или не может достучаться до backend API.

## Быстрый запуск

### 1. Убедитесь, что backend запущен

```bash
cd backend
docker-compose up -d
# Или локально:
npm run dev
```

Проверьте, что backend доступен:
```bash
curl http://localhost:3001/health
```

### 2. Настройте переменные окружения

Создайте или обновите `.env` файл в директории `backend/`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
TELEGRAM_BOT_USERNAME=your_bot_username
API_URL=http://localhost:3001
```

**Важно:** Если backend запущен в Docker, а бот локально, используйте:
- Для macOS/Windows: `http://host.docker.internal:3001`
- Или IP адрес вашего компьютера: `http://192.168.1.119:3001` (замените на ваш IP)

### 3. Установите зависимости (если еще не установлены)

```bash
cd backend
npm install
```

### 4. Запустите бота

```bash
# Один раз
npm run bot

# Или с автоперезагрузкой (для разработки)
npm run bot:dev
```

Вы должны увидеть:
```
🤖 Запуск бота: your_bot_username
📡 Backend API: http://localhost:3001
🤖 Telegram bot is running...
```

## Проверка работы

1. Откройте приложение на телефоне
2. Нажмите "Войти через Telegram"
3. В Telegram нажмите кнопку "Начать" (Start)
4. Бот должен отправить сообщение: "✅ Верификация успешна!"
5. Приложение должно автоматически получить токены и войти

## Отладка

### Проверьте логи бота

Если бот запущен, вы увидите логи в консоли:
- `Error verifying user:` - ошибка при проверке токена
- `Error: connect ECONNREFUSED` - бот не может достучаться до backend

### Проверьте доступность API

```bash
# Проверка endpoint для бота
curl -X POST http://localhost:3001/api/auth/telegram/bot/verify-token \
  -H "Content-Type: application/json" \
  -d '{"verifyToken":"test"}'
```

### Проверьте токен бота

Убедитесь, что токен в `.env` правильный. Получить новый можно у [@BotFather](https://t.me/BotFather).

## Альтернатива: Запуск бота в Docker

Можно добавить бота как отдельный сервис в `docker-compose.yml`:

```yaml
bot:
  build:
    context: .
    dockerfile: Dockerfile
  container_name: our-village-bot
  environment:
    - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    - TELEGRAM_BOT_USERNAME=${TELEGRAM_BOT_USERNAME}
    - API_URL=http://app:3000
  depends_on:
    - app
  networks:
    - our-village-network
  command: node telegram-bot-example.js
```


